[TOC]

<h1 align="center">知识碎片</h1>

## 零零散散

mysql -h127.0.0.1 -uroot -p123456

Axios 实例：https://www.axios-http.cn/docs/instance

表 vs. 模型：  
表（Table）：是一个集合，包含多条记录。所以表名用复数 examples。  
模型（Model）：是代表这个集合中单条记录的蓝图或实例。所以模型名用单数 example。

模型/服务：通常定义为类，使用单数 ExampleModel, ExampleService  
路由：处理 HTTP 请求，对应资源集合，使用复数 examples_routes.py

```
components/
├── ExampleDialog.vue         # 弹窗容器
├── ExampleForm.vue           # 纯表单组件
└── ExampleDetail.vue         # 详情展示组件
```

统计工作区中被 Git 管理的文件大小  
git ls-files -z | xargs -0 du -hc | tail -1

按文件大小排序显示  
git ls-files | xargs ls -l | sort -k5 -nr

显示每个文件的大小（人类可读格式）  
git ls-files | xargs du -h | sort -hr
docs 文件最大

子模块
```
cd backend/app
git submodule add git@github.com:iewiewiew/wei-notebook.git ./docs
git submodule update -f --remote docs

git submodule add git@gitee.com:iewiewiew/api-autotest.git ./api-autotest
git submodule update -f --remote api-autotest

git submodule deinit api-autotest
git rm api-autotest
rm -rf .git/modules/api-autotest
```



## 待办列表

Python Flask + Vue Web 开发，提一些问题检查掌握情况

作为一个测试开发工程师，现需要开发一个给互联网 QA/测试/开发工程师使用的效能平台，用于提升测试开发效率，解决测试痛点，输出详细功能设计方案

技术栈：Flask + Vue（element-ui-plus）+ MySQL

文件目录参考下面：
后端命名规范

```
backend/app/models/example_model.py
backend/app/services/example_service.py
backend/app/routes/example_routes.py
```

前端命名规范

```
frontend/src/services/exampleService.js
frontend/src/stores/exampleStore.js
frontend/src/views/ExampleView.vue
frontend/src/components/ExampleDialog.vue
frontend/src/router/index.js
frontend/src/layouts/AppLayout.vue
```

@todo

1. 测试平台的云真机
2. 集成子模块项目 backend/app/api-autotest，在自动化测试-测试用例调用 main.py 执行测试用例，使得项目解耦，先给出代码写入 .tmp/tmp/tmp.md，不要直接修改
3. 在 test-platform/backend 实现把当前项目所需要的依赖写入 requirements.txt 